# 🚀 上线操作手册（5 分钟完成）

网站已经做好，只差最后一步：把它放进 **GitHub 免费空间**。国内用户可通过高速 CDN 正常访问，**无需备案、无需花钱**。

## 方式 A：把令牌交给助手部署（最简单，推荐）

> 适合不想安装任何软件的您。整个部署由助手代劳，完成后把网址发给您。

1. **注册账号**：用浏览器打开 <https://github.com> → 右上角 **Sign up** → 用邮箱注册（邮箱会收到验证邮件，点一下链接即可）。
2. **生成令牌（上传钥匙）**：
   - 登录后点右上角**头像** → **Settings**
   - 左侧滚到最下面 → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
   - 点 **Generate new token (classic)**：
     - Note：随便填（如 `music`）
     - Expiration：选 **90 days** 或 **No expiration**
     - 只勾选 **public_repo**（它会自动勾选下面几项，正常现象）
   - 点最下面的 **Generate token** → 复制 `ghp_` 开头的一串字符（只显示这一次）
3. **发给助手**：把令牌粘贴发给助手，并告诉他您想要的：
   - 仓库名（默认 `music`，可留空）
   - 后台管理密码（默认 `admin888`，可自定义）
   - 网站名称（默认「悦音」）

✅ 助手部署完成后会发给您两个网址：**首页链接**（分享给客户）和**后台链接**（您自己管理音乐用）。

## 方式 B：自己一键部署

> 适合愿意安装 Node.js 的您，全程一个命令自动完成。

1. 完成方式 A 的第 1、2 步（注册 + 生成令牌）。
2. 下载安装 Node.js（国内高速镜像）：<https://npmmirror.com/mirrors/node/> → 选最新 **LTS 版本** 的 `.msi` 文件 → 一路下一步。
3. 打开音乐网站文件夹，在地址栏输入 `cmd` 回车，然后输入：
   ```
   npm run deploy
   ```
4. 按提示操作：粘贴令牌（屏幕不显示是正常的）→ 回车 → 输入仓库名/密码/网站名 → 回车。
5. 看到「部署完成」即成功，把打印出来的两个网址保存下来。

## 部署完成后，您会得到什么

| 网址 | 用途 |
| --- | --- |
| `https://cdn.jsdelivr.net/gh/用户名/仓库名@main/index.html` | **首页**（国内访问快，建议分享这个） |
| `https://用户名.github.io/仓库名/` | 首页备用地址 |
| 同上把 `index.html` 换成 `admin.html` | **后台管理**（输入密码进入） |

- **登录后台**：打开后台网址 → **只输入管理密码**（无需任何令牌）
- **上传音乐**：先选好栏目 → 选择音频文件**即自动上传**（标题默认用文件名，可在列表点「改名」修改）
- **删除音乐**：后台列表里点红色「删除」
- **单曲专属链接**：每首音乐都有「🔗 链接」按钮 → 点击自动复制该曲目的专属页面网址 → 发给想给的人，对方打开就是这首音乐的独立页面（可试听可下载）。**客户音乐**默认不在首页展示，正是通过这个方式一对一分享
- **改密码**：后台右上角「设置」→ 输入两次新密码 → 自动同步到云端（换设备也通用）
- **界面没更新到最新版？** 按 Ctrl+F5 强制刷新一次即可（浏览器可能缓存了旧文件）。日常管理**建议用 GitHub Pages 后台地址**（每 10 分钟自动更新）：`https://你的用户名.github.io/仓库名/admin.html`

## 三点须知（重要）

1. **单文件 ≤ 19MB**：免费 CDN 的限制，建议用 MP3 格式；
2. **空间是公开的**：客户音乐只是隐藏了下载按钮，重要私密文件请勿上传；
3. **令牌像银行卡密码**：只保存在您自己的浏览器里，不要发给陌生人；泄露或忘记可随时到 GitHub 重新生成。
