# 悦音 · 音乐网站

一个简洁大气的音乐网站：**音频播放器式首页** + **密码后台管理**，免注册、直接下载，手机电脑都好用。

## 功能一览

| 功能 | 前台（访客） | 后台（管理员，需密码） |
| --- | --- | --- |
| 在线试听 | ✅ 全部四个栏目 | ✅ |
| 下载 | ✅ 全部四个栏目 | ✅ 全部栏目 |
| 上传 / 删除 | ❌ 无任何入口 | ✅ 分栏目上传、删除 |

- 首页四个栏目：**客户音乐 / 背景音乐 / 女声 / 男声**
- 底部悬浮播放器：播放/暂停、上一首/下一首、进度条拖动、正在播放动效
- 大号下载按钮，带实时下载进度
- 免注册、免登录，打开即听、点击即下
- 手机自适应（响应式布局 + 底部播放条）

## 目录结构

```
music-website/
├── index.html          前台首页
├── admin.html          后台管理入口（密码保护）
├── assets/             样式与脚本
│   ├── config.js       网站配置（部署脚本自动改写）
│   ├── style.css       首页样式
│   ├── app.js          首页逻辑（播放器/下载）
│   ├── admin.css       后台样式
│   └── admin.js        后台逻辑（上传/删除）
├── music/              音乐文件与曲库清单 index.json（演示曲可删除）
├── dev-server.mjs      本地预览服务器（零依赖）
├── deploy/deploy.mjs   一键部署到 GitHub 的脚本
└── scripts/            开发辅助脚本（不会上传到网站）
    ├── gen-seed.mjs        生成演示音频（可选）
    ├── mock-github.mjs     GitHub 后台 API 模拟服务器（自动化测试用）
    ├── mock-github-full.mjs 完整 GitHub API 模拟服务器（部署脚本测试用）
    ├── test-github-mode.mjs 后台全流程自动化测试（node scripts/test-github-mode.mjs）
    ├── test-frontend.mjs    前台流程自动化测试（node scripts/test-frontend.mjs）
    └── test-deploy.mjs      部署脚本全流程测试（node scripts/test-deploy.mjs）
```

## 一、本地预览（可选）

需要电脑安装 [Node.js](https://nodejs.org/zh-cn)（官网下载安装即可）。在项目文件夹打开终端：

```
node dev-server.mjs
```

然后浏览器打开 `http://localhost:8080`（首页）、`http://localhost:8080/admin.html`（后台，默认密码 `admin888`）。

## 二、上线部署（免费空间，国内可访问）

网站采用 **GitHub 仓库作为免费空间**，通过 **jsDelivr CDN** 在国内高速访问。只需三步：

### 第 1 步：注册 GitHub 账号（免费）

打开 <https://github.com> → 右上角 **Sign up** → 按提示用邮箱注册并验证。

### 第 2 步：生成访问令牌（相当于“上传钥匙”）

1. 登录 GitHub，点右上角**头像** → **Settings**
2. 左侧最下面 **Developer settings** → **Personal access tokens** → **Tokens (classic)**
3. 点 **Generate new token (classic)**：
   - Note 随便填（如 `music`）
   - Expiration 建议选 90 天或 No expiration
   - 只勾选 **public_repo**（勾上它会自动勾选下面几个子项，正常）
4. 拉到最下面点 **Generate token**，复制 `ghp_` 开头的令牌（只显示一次，先粘贴保存到记事本）

### 第 3 步：运行一键部署脚本

```
npm run deploy
```

按提示输入：令牌（粘贴时屏幕不显示，属正常）、仓库名（如 `music`）、后台密码、网站名称。脚本会自动：创建公开仓库 → 上传网站 → 启用 GitHub Pages → 输出网址。

部署完成后会显示两个网址：

- **国内推荐**：`https://cdn.jsdelivr.net/gh/你的用户名/仓库名@main/index.html`
- **备用域名**：`https://你的用户名.github.io/仓库名/`

后台管理地址 = 上面的地址把 `index.html` 换成 `admin.html`。

## 三、日常使用

### 前台（访客视角）
打开首页链接即可。**四个栏目均可在线试听 + 点大按钮直接下载**，无需注册登录。

### 后台（你自己）
1. 打开后台网址 → **输入管理密码即可**（无需再粘贴令牌，令牌已用密码加密存放在网站配置里，登录时自动解密）
2. 上传：选文件 → 填曲目名称 → 选栏目（背景音乐/女声/男声/客户音乐）→ 点“上传到「XX」”，带进度条
3. 删除：点列表里的红色“删除”按钮，二次确认后删除（前台同步消失）
4. “设置”里可以修改后台密码（**改完自动同步到云端，换设备也能用新密码登录**）
5. 手机上也能管理：手机浏览器打开后台网址，输入密码即可上传/删除

### 分享给客户
把首页链接发给客户即可。演示用的两首“演示曲”可在后台上传真实音乐后删除。

## 四、重要说明（请务必阅读）

1. **免费方案的代价——仓库公开**：GitHub 免费公开仓库 + 免费 CDN 才无需备案、国内可访问。所有上传的音乐文件技术上都可以通过直链取得，**重要、私密的文件请不要上传**。
2. **单文件 ≤ 19MB**：jsDelivr 免费 CDN 单文件上限 20MB。建议上传 MP3（320kbps 的 5 分钟歌曲约 12MB，足够用）。
3. **列表更新有缓存**：上传/删除后，jsDelivr 会有几分钟缓存延迟，刷新几次或稍等即可。新上传的文件本身是新的网址，立即生效。
4. **国内访问**：建议使用 jsDelivr 链接（`cdn.jsdelivr.net/gh/...`）；`用户名.github.io` 链接在部分网络下可能打不开或较慢，作为备用。
5. **密码就是唯一钥匙**：上传令牌已用管理密码加密（PBKDF2+AES-256-GCM）存放在公开配置中，只有输入正确密码才能解出。**请保管好密码、不要告诉他人**；修改密码请在后台“设置”里进行（自动同步云端）。
6. **修改网站名称/联系提示**：直接编辑 GitHub 仓库里的 `assets/config.js` 文件（网站名 `siteName`、联系提示 `contact`），或在本地改完后重跑部署脚本。
7. **令牌 90 天到期**：classic 令牌到期后上传会报错，重新生成一个令牌交给技术支持，一分钟即可重新加密发布（本地工具：`DSH_TOKEN=新令牌 DSH_PASSWORD=密码 node scripts/encrypt-pat.mjs` 后重新发布 `assets/config.js`）。

## 五、常见问题

**Q：上传失败提示“令牌无效或已过期”？**
A：classic 令牌到期（90 天）或失效。重新生成一个令牌，参照“重要说明”第 7 条重新加密发布即可（可联系技术支持协助）。

**Q：上传成功但首页看不到？**
A：等 1~2 分钟（CDN 缓存）后刷新。也可以上传后点开对应栏目刷新一次。

**Q：能上传 19MB 以上的音频吗？**
A：免费方案不支持。可压缩音质（MP3 192~256kbps），或将来换收费空间。

**Q：换设备后后台能进吗？**
A：能。任何设备打开后台网址，输入管理密码即可（数据都存在云端，密码在“设置”里修改后所有设备通用）。

**Q：忘记后台密码怎么办？**
A：联系技术支持，用 GitHub 令牌重新生成加密配置（本地工具：`DSH_TOKEN=令牌 DSH_PASSWORD=新密码 node scripts/encrypt-pat.mjs` 并重新发布 `assets/config.js`）。

**Q：想用自己的域名？**
A：可购买域名后在 GitHub 仓库 Settings → Pages 里绑定，或在 README 末尾联系技术支持。
